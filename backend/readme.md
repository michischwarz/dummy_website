# Install dependencies

It's important to install the needed packages to the Python version used by the web app, in this case 3.11:

```bash
pip3.11 install --user flask 
```
or

```bash
pip3.11 install --user -r requirements.txt
```
